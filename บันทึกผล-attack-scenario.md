# บันทึกผลการทดสอบ Attack Scenario

# 3.4 Payload ขนาดใหญ่ผิดปกติ

ทดสอบส่ง POST /api/v1/students โดยส่งข้อมูลที่มีขนาดเกิน 10KB พบว่าเซิร์ฟเวอร์ตอบกลับด้วย Status Code 413 Payload Too Large แสดงว่า express.json({ limit: "10kb" }) สามารถจำกัดขนาดได้ตามที่กำหนด และช่วยป้องกันการส่ง Payload ที่มีขนาดใหญ่ผิดปกติได้

# 3.5 Type Confusion

ทดสอบส่งค่า name เป็น Object พบว่าระบบสามารถรับข้อมูลดังกล่าวได้ เนื่องจากการตรวจสอบข้อมูลปัจจุบัน ตรวจว่าฟิลด์มีค่าหรือไม่ แต่ไม่ได้ตรวจสอบชนิดข้อมูล ข้อสังเกตคือ ระบบควรมีการตรวจสอบ Type ข้อมูลให้ละเอียดมากขึ้น เพื่อป้องกันข้อมูลที่ไม่ถูกต้อง (ซึ่งจะได้รับการแก้ไขอย่างเป็นระบบด้วยไลบรารี Zod ในสัปดาห์ที่ 13)
